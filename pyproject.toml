[project]
name = "kodo"
version = "0.1.0"
description = "Kodo — build a local LLM library, then run, chat, and serve it — fully on-box"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.138.2",
    "fastmcp>=3.4.2",
    "hf-xet>=1.5.1",
    "httpx>=0.28.1",
    "huggingface-hub>=1.21.0",
    # Bundled first-party MCP tool servers (pure-stdlib / light) — kodo ships with a
    # working tool set out of the box, so these are base deps, not extras. They're
    # workspace members (see [tool.uv.sources]); a plain PyPI `pip install kodo` can't
    # resolve them because they're unpublished. That's by design: kodo is installed from
    # this workspace (`uv sync` / `uv tool install -e .`), where they resolve as editable
    # siblings — a standalone PyPI wheel is not a supported install path. The heavier
    # `kodo-mcp-web` (Playwright) and the dev-only `kodo-benchmark` are extras instead.
    "kodo-mcp-datetime",
    "kodo-mcp-exec",
    "kodo-mcp-files",
    "kodo-mcp-memory",
    "kodo-mcp-network",
    "kodo-mcp-search",
    "kodo-mcp-shell",
    "kodo-mcp-weather-yr",
    "kodo-mcp-utils",
    # Kokoro-82M (ONNX) — the lightweight, cross-platform in-chat voice. A base dep (not an
    # extra) so "Listen"/speak works out of the box on every install; the design treats it as
    # always-available, and it's self-contained (kokoro-onnx bundles espeak-ng via
    # espeakng_loader, soundfile bundles libsndfile — no system packages). The heavier
    # Apple-only mlx-audio runtime (Dia/Whisper) stays the opt-in `voice` extra.
    "kokoro-onnx>=0.4.9",
    "pluginkit>=0.4.7",
    "pydantic-settings>=2.14.2",
    "soundfile>=0.13.1",
    "textual>=0.85",
    "typer>=0.25.1",
    "uvicorn[standard]>=0.49.0",
]

[project.optional-dependencies]
# Apple-Silicon MLX runtimes. Optional + platform-gated: kodo spawns these as
# OpenAI servers (it does not import them), and they have no Linux wheels, so
# they must never be hard deps. mlx-vlm serves multimodal MLX checkpoints that
# text-only mlx-lm can't load. Install with `uv sync --extra mlx` on an M-series
# Mac (a no-op elsewhere). llama.cpp's `llama-server` stays external (a C++
# binary; `brew install llama.cpp`).
mlx = [
    "mlx-lm>=0.31.3; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "mlx-vlm>=0.6.3; sys_platform == 'darwin' and platform_machine == 'arm64'",
    # transformers 5.13 broke AutoTokenizer.register (now requires a config class, not a
    # string) — mlx-lm 0.31.3 passes a string and crashes at import, so any MLX model fails
    # to load. Cap below 5.13 until mlx-lm ships a compatible register call. Remove when it does.
    "transformers>=5.5,<5.13; sys_platform == 'darwin' and platform_machine == 'arm64'",
]

# NOTE: Kokoro-82M (ONNX) TTS moved to [project.dependencies] — it's the always-available
# in-chat voice, no longer an opt-in extra. See the note there.

# Full voice runtime via mlx-audio (Apple Silicon): Dia (dialogue + cloning), Kokoro,
# Qwen3-TTS, and Whisper STT, served through mlx-audio's OpenAI /v1/audio server which kodo
# spawns + proxies. mlx-audio is gated (mlx, Apple-only, no Linux wheels); its per-model
# helpers (misaki G2P for Kokoro, soundfile) are cross-platform. On Linux, the base
# Kokoro-ONNX dep covers TTS. Install with `uv sync --extra voice` on an M-series Mac.
voice = [
    "mlx-audio>=0.4.4; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "misaki[en]>=0.9.4",  # grapheme->phoneme for Kokoro (English backend)
    "soundfile>=0.13.1",
    # mlx-audio pulls in mlx-lm, which crashes at import on transformers 5.13 (the
    # AutoTokenizer.register break — see the `mlx` extra). A project using [voice] without
    # [mlx] would otherwise resolve an unbounded transformers and Whisper STT dies. Cap here
    # too so [voice] alone is self-consistent. Keep in lockstep with the `mlx` extra.
    "transformers>=5.5,<5.13; sys_platform == 'darwin' and platform_machine == 'arm64'",
]

# Web reader MCP server (kodo-mcp-web): reads a page in a headless browser and returns
# Markdown. Optional because Playwright + Chromium are heavy (a ~150 MB browser via
# `playwright install chromium`), unlike the bundled stdlib servers. Install with
# `uv sync --extra web` (or `make install-web`, which also fetches the browser). The pure
# stdlib/light servers stay bundled in [project.dependencies]; this one is opt-in.
web = [
    "kodo-mcp-web",
]

# Benchmarking / eval harness (kodo-benchmark): contributes the `kodo benchmark` command
# for measuring model quality across a suite. It's a dev/eval tool, not part of the runtime
# an end user needs to chat or serve, so it's opt-in rather than a base dependency (which
# also keeps it out of the app's wheel Requires-Dist). Install with `uv sync --extra benchmark`
# (or add `benchmark` to the `uv tool install -e .[...]` extras to expose `kodo benchmark`
# on the global command).
benchmark = [
    "kodo-benchmark",
]

[project.scripts]
kodo = "kodo.cli:main"

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

# uv workspace: the app (this package) plus one bundled MCP server per package under
# packages/kodo-mcp-*/. New servers (e.g. kodo-mcp-weather-yr) are sibling members and
# join automatically via the glob; add each as a dependency + source below to bundle it.
[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
kodo-mcp-datetime = { workspace = true }
kodo-mcp-exec = { workspace = true }
kodo-mcp-files = { workspace = true }
kodo-mcp-memory = { workspace = true }
kodo-mcp-network = { workspace = true }
kodo-benchmark = { workspace = true }
kodo-sandbox = { workspace = true }
kodo-mcp-search = { workspace = true }
kodo-mcp-shell = { workspace = true }
kodo-mcp-weather-yr = { workspace = true }
kodo-mcp-utils = { workspace = true }
kodo-mcp-web = { workspace = true }  # optional (the `web` extra); heavy Playwright deps

[tool.fastapi]
# Let `fastapi dev` / `fastapi run` auto-detect the app.
entrypoint = "kodo.app:app"

[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint]
fixable = ["ALL"]
select = ["E", "W", "F", "I", "D"]
ignore = ["D203", "D213"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"]
"packages/*/tests/**/*.py" = ["D"]
"**/__init__.py" = ["D104"]
"src/**/*.py" = ["D102", "D105", "D107"]
"packages/*/src/**/*.py" = ["D102", "D105", "D107"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
docstring-code-format = true
docstring-code-line-length = "dynamic"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "packages"]  # recurse packages/ to pick up every member's tests/
norecursedirs = [".git", ".venv", "__pycache__"]
# Exclude the live e2e tests by default so a bare `pytest` / `uv run pytest` stays
# hermetic (they spawn a real runtime and need a working local model). Opt in with
# `pytest -m slow`. `make test` passes its own `-m "not slow"`, which still works.
addopts = "-m 'not slow'"
markers = [
  "slow: live end-to-end tests that start a real runtime (deselect with -m 'not slow')",
]

[tool.coverage.run]
branch = true
dynamic_context = "test_function"
relative_files = true
source = ["kodo", "kodo_mcp_datetime", "kodo_mcp_memory", "kodo_mcp_weather_yr", "kodo_mcp_exec", "kodo_mcp_files", "kodo_benchmark", "kodo_sandbox"]

[tool.coverage.report]
exclude_also = ["if TYPE_CHECKING:"]
precision = 2
show_missing = true
skip_covered = true

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_unused_ignores = true
strict_equality = true
# Member roots go on MYPYPATH via the Makefile (glob-expanded) so each flat module
# (packages/kodo-mcp-*/kodo_mcp_*.py) resolves to a clean, hyphen-free module name.
mypy_path = ["src"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

[[tool.mypy.overrides]]
# Optional `tts` extra: untyped (soundfile/espeakng_loader) or absent when the
# extra isn't synced. Treat as Any so `make check` passes with or without it.
module = [
    "soundfile.*",
    "espeakng_loader.*",
    "kokoro_onnx.*",
    "mlx.*",
    "mlx_audio.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# Optional `web` extra: playwright ships types but trafilatura doesn't, and both are
# absent in the base env. Treat as Any so `make check` passes with or without the extra.
module = ["playwright.*", "trafilatura.*"]
ignore_missing_imports = true

[tool.pyright]
include = ["src", "tests", "packages/*/src", "packages/*/tests"]
exclude = ["**/.venv"]
pythonVersion = "3.13"
typeCheckingMode = "strict"
useLibraryCodeForTypes = true
reportPrivateUsage = false
reportUnusedFunction = false
reportUnknownMemberType = false
reportUnknownArgumentType = false
reportUnknownParameterType = false
reportUnknownVariableType = false
reportMissingTypeArgument = false
reportMissingTypeStubs = false
reportUnknownLambdaType = false
reportMissingImports = "warning"
reportMissingModuleSource = false

[dependency-groups]
dev = [
    "coverage>=7.14.3",
    "httpx>=0.28.1",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.6",
    "mkdocstrings[python]>=1.0.4",
    "mypy>=2.1.0",
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.20",
]
